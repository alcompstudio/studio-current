import React from "react";

/**
 * Это временный тестовый компонент для проверки системы документации.
 * Его можно безопасно удалить после тестирования.
 */
const TemporaryTestComponent = () => {
  return (
    <div data-oid="jw1.azt">
      <p data-oid="9tma6x1">Это временный тестовый компонент.</p>
    </div>
  );
};

export default TemporaryTestComponent;
